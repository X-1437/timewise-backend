## 1. 文档定位

本清单用于作为“时间序列数据分析助手”项目的正式交付清单。

使用场景：
- 向领导汇报交付范围
- 向接手方做正式移交
- 作为项目归档与签收依据

本清单遵循当前已确认的交付策略：
- 暂不调整 Git 分支策略
- 先按现有仓库结构完成正式交付
- 正式交付主线采用 `timeswise + timewise-backend/mcp/src`

## 2. 交付总原则

本项目交付的不是单一代码目录，而是四类资产共同组成的完整交付包：

- 正式运行代码
- 全周期文档资产
- 历史基础与阶段性保留项
- 交接说明与边界口径

需要特别强调：
- 第一周到第五周的 `md` 文档属于正式交付物
- `app/` 属于历史业务基础，应保留说明
- `mcp/src` 是当前正式后端交付目录
- `mcp/` 下各周目录属于阶段性记录，不替代正式交付目录

## 3. 正式运行交付物

### 3.1 前端正式交付代码

交付路径：
- `d:\trae_workspace\data\github\timeswise`

交付内容：
- React 前端代码
- 页面组件
- API 调用逻辑
- 构建配置文件
- 依赖清单

交付说明：
- 该目录为当前正式前端交付目录
- 用于部署和运行当前对话式产品前端

### 3.2 后端正式交付代码

交付路径：
- `d:\trae_workspace\data\github\timewise-backend\mcp\src`

交付内容：
- 后端入口
- API 路由
- Service 层
- DAL 层
- LLM 层
- MCP 层
- 数据模型
- 脚本与测试代码
- 环境变量模板
- 依赖清单

交付说明：
- 该目录为当前正式后端交付目录
- 用于部署和运行当前对话式产品后端
- 当前交付以该目录为准，不以 `app/` 为准

### 3.3 环境变量与依赖文件

应一并交付的关键文件包括：

前端：
- `d:\trae_workspace\data\github\timeswise\package.json`
- `d:\trae_workspace\data\github\timeswise\package-lock.json`

后端：
- `d:\trae_workspace\data\github\timewise-backend\mcp\src\.env.example`
- `d:\trae_workspace\data\github\timewise-backend\mcp\src\requirements.txt`

## 4. 全周期文档交付物

### 4.1 GitHub 镜像文档

应交付以下目录中的全部 `md` 文档：

- `d:\trae_workspace\data\github\timewise-backend\docs\first_week`
- `d:\trae_workspace\data\github\timewise-backend\docs\second_week`
- `d:\trae_workspace\data\github\timewise-backend\docs\third_week`
- `d:\trae_workspace\data\github\timewise-backend\docs\fourth_week`
- `d:\trae_workspace\data\github\timewise-backend\docs\fifth_week`

交付说明：
- 该部分用于体现 GitHub 侧归档结果
- 该部分是正式对外交付的文档主目录

### 4.2 本地学习记录文档

应交付以下目录中的全部 `md` 文档及周度过程资产：

- `d:\trae_workspace\data\file\first_week`
- `d:\trae_workspace\data\file\second_week`
- `d:\trae_workspace\data\file\third_week`
- `d:\trae_workspace\data\file\fourth_week`
- `d:\trae_workspace\data\file\fifth_week`

交付说明：
- 该部分用于保留完整建设过程
- 该部分与 GitHub 镜像文档共同组成全周期项目证据链

### 4.3 文档交付价值

第一周到第五周文档共同用于证明以下事项：

- 项目并非单点开发，而是完整五周建设形成
- 项目是基于原有 Web 业务基础逐步演进而来
- 每周均有明确目标、执行过程与阶段成果
- 最终验收、发布与收口过程真实存在且可追溯

## 5. 历史基础与保留项

### 5.1 原有业务基础

保留路径：
- `d:\trae_workspace\data\github\timewise-backend\app`

保留说明：
- 该目录是原有 Web 形态产品后端实现
- 它是当前项目的重要历史基础与复用来源
- 该目录应保留说明，但不作为本次正式部署主目录

### 5.2 阶段性开发基线

保留路径：
- `d:\trae_workspace\data\github\timewise-backend\mcp\` 下各周阶段性目录

重点说明：
- `mcp/fifth_week` 是当前正式目录整理前最完整的阶段性基线

保留说明：
- 该部分用于保留阶段性代码与镜像记录
- 该部分不替代 `mcp/src` 的正式交付角色

## 6. 本次已完成的关键核验

本次已完成以下关键核验，用于证明交付不是只看文档、不看实物：

前端：
- 在 `d:\trae_workspace\data\github\timeswise` 执行 `npm run build`
- 结果：通过

后端：
- 在 `d:\trae_workspace\data\github\timewise-backend\mcp\src` 执行 `python -m compileall -q .`
- 结果：通过

后端测试：
- 在 `d:\trae_workspace\data\github\timewise-backend\mcp\src` 执行 `python -m unittest -q tests.test_full_flow_intent tests.test_image_artifacts`
- 结果：15 项测试全部通过

## 7. 交接时必须说明的边界

交接时必须明确以下口径：

- 正式前端交付目录是 `timeswise`
- 正式后端交付目录是 `timewise-backend/mcp/src`
- `app/` 是历史业务基础，不是本次正式部署目录
- `mcp/` 下各周目录是阶段性资产，不是正式部署目录
- 第一周到第五周的 `md` 文档属于正式交付物

## 8. 建议的交付顺序

建议按以下顺序执行正式交接：

第一步：
- 交付本清单和项目整体交付说明

第二步：
- 交付前端正式代码目录 `timeswise`

第三步：
- 交付后端正式代码目录 `timewise-backend/mcp/src`

第四步：
- 交付第一周到第五周全部文档目录

第五步：
- 说明 `app/` 和 `mcp/` 下各周目录的保留性质与边界

第六步：
- 交付环境变量模板、依赖文件与启动说明

第七步：
- 由接收方完成部署验证与最小验收

第八步：
- 完成最终签收确认

## 9. 最终签收口径

本项目正式交付时，建议按以下口径签收：

- 已完成第一周到第五周的完整建设与收口
- 已形成正式运行代码、全周期文档和历史基础说明三类交付物
- 当前正式交付主线明确
- 当前已具备进入部署接收阶段的条件
