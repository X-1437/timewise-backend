# 第三周任务完成说明（阶段性）

## 1. 文档目的

本文件用于记录第三周“接入MongoDB与调用MCP + 真实大模型集成”的阶段性完成情况、环境与运行方式、关键决策与问题处理，以便复盘与后续在GitHub仓库镜像归档。

## 2. 项目最终目的回顾（对齐）

- 项目名称：鸿溯 - 时间序列数据分析助手
- 项目最终目的：通过该项目完成产品开发全流程生命周期闭环学习与实践（从需求到上线的闭环），形成可复用的架构与工程化资产。
- 第三周目标：完成 MongoDB 持久化 + 真实大模型集成 + MCP工具调用链路打通（后续周次在此架构之上持续扩展与复用）。

## 3. 代码与文档管理方案回顾（对齐）

依据：[GitHub仓库管理方案.md](file:///D:/trae_workspace/data/file/all/GitHub%E4%BB%93%E5%BA%93%E7%AE%A1%E7%90%86%E6%96%B9%E6%A1%88.md)

- 学习记录目录（本地）：`D:\trae_workspace\data\file\third_week\`
- 工程化仓库目录（GitHub镜像）：`D:\trae_workspace\github\timewise-backend\`
- 本周工作流：
  - 在学习记录目录内完成文档与代码（可回溯）
  - 本周结束后将文档复制到 `timewise-backend/docs/third_week/`
  - 将代码复制到 `timewise-backend/mcp/third_week/`（Demo/阶段性代码）或按工程化规划落位

## 4. 环境准备与验证结果

### 4.1 MongoDB

- MongoDB 服务已存在并运行（Windows Service：MongoDB Server）。
- 说明：命令行 `mongod` 未加入 PATH 不影响服务运行；后端通过 URI 直接连接即可。

### 4.2 Python隔离环境（Conda）

- 采用环境：`hongsu-mcp`
- 原则：尽量复用既有隔离环境，避免污染 base 环境。

### 4.3 依赖安装结果

- 已在 `hongsu-mcp` 环境内安装第三周所需依赖
- 解决的问题：
  - `motor` 与 `pymongo` 版本依赖冲突：将 `pymongo` 调整为 `4.9.2`
  - `pydantic` 版本约束冲突：改为 `pydantic>=2.11,<3`、`pydantic-settings>=2.8,<3`，以满足 `mcp` 依赖约束

依赖文件位置：
- [requirements.txt](file:///D:/trae_workspace/data/file/third_week/code/requirements.txt)

## 5. 第三周代码框架落地情况（已完成）

### 5.1 代码目录

代码位置：`D:\trae_workspace\data\file\third_week\code\`

已落地的关键文件：
- 后端入口：[backend.py](file:///D:/trae_workspace/data/file/third_week/code/backend.py)
- 配置读取：[config.py](file:///D:/trae_workspace/data/file/third_week/code/config.py)
- 环境变量模板：[.env.example](file:///D:/trae_workspace/data/file/third_week/code/.env.example)

### 5.2 分层架构实现情况

- API 路由层：`api/`（sessions/messages/files）
- Service 层：`services/`（session/message/file/chat）
- DAL 层：`dal/`（抽象接口 + MongoDB实现）
- LLM 层：`llm/`（OpenAI调用 + 降级规则）
- MCP 层：`mcp_layer/`（MCP server + client）
- Models：`models/`（Pydantic模型）

说明：本项目原设计希望目录名为 `mcp/`，但由于 Python 依赖也叫 `mcp`，会产生导入冲突，因此本周代码采用 `mcp_layer/` 作为本地目录名，避免覆盖第三方包。

## 6. MongoDB持久化与时间序列集合

### 6.1 自动初始化集合与索引

后端启动时会自动创建/确保：
- `sessions`（索引：updatedAt desc）
- `messages`（索引：sessionId + timestamp）
- `files`（索引：sessionId）
- `time_series_data`（time series collection，索引：metadata.fileId）

实现位置：
- [connection.py](file:///D:/trae_workspace/data/file/third_week/code/dal/mongodb/connection.py)

### 6.2 CSV入库策略（方案2）

- CSV原文件不落本地磁盘
- 文件信息写入 `files`
- CSV逐行解析后写入 `time_series_data`（每行一条文档）

实现位置：
- [file_service.py](file:///D:/trae_workspace/data/file/third_week/code/services/file_service.py)

## 7. MCP链路打通情况

### 7.1 MCP Server（stdio模式）

- MCP工具定义与模拟实现：`mcp_layer/tools/mcp_tools.py`
- 修复点：mcp 1.27.0 的 `InitializationOptions` 需要显式传入 capabilities（否则启动失败）

实现位置：
- [mcp_tools.py](file:///D:/trae_workspace/data/file/third_week/code/mcp_layer/tools/mcp_tools.py)

### 7.2 MCP Client（后端启动时连接）

实现位置：
- [mcp_layer/client.py](file:///D:/trae_workspace/data/file/third_week/code/mcp_layer/client.py)

## 8. 真实大模型接入情况（阶段性）

### 8.1 代码已接入

- 通过 `llm/client.py` 调用 OpenAI Chat Completions（tools模式）进行意图识别
- 无 API Key 时自动降级为规则匹配，保证链路可运行

实现位置：
- [llm/client.py](file:///D:/trae_workspace/data/file/third_week/code/llm/client.py)

### 8.2 待完成项：配置真实 API Key 并验证“真实大模型→选工具→MCP调用”闭环

- 当前环境检测：OpenAI provider 未配置（需要你在IDE集成里写入真实 Key，或在 `.env` 文件写入）。
- 建议路径（任选其一）：
  - `D:\trae_workspace\data\file\third_week\code\.env`
  - 或 `D:\trae_workspace\data\.env`

对应配置模板见：
- [.env.example](file:///D:/trae_workspace/data/file/third_week/code/.env.example)

## 9. 服务启动与自测方式

### 9.1 启动后端

在 `hongsu-mcp` 环境下启动：

```bash
conda run -n hongsu-mcp python -m uvicorn backend:app --host 0.0.0.0 --port 8000
```

### 9.2 验证接口可用

- Swagger：`http://localhost:8000/docs`
- 已提供接口：
  - `POST /api/v1/sessions`
  - `GET /api/v1/sessions`
  - `POST /api/v1/files/upload`
  - `POST /api/v1/chat/messages`

## 10. 当前阶段性结论

已完成（第三周“框架确定并跑通”的关键目标）：
- 分层架构骨架落地并可启动
- MongoDB 连接、集合、索引、时间序列集合初始化
- CSV解析入库（方案2：只入MongoDB）
- MCP server/client链路启动并可复用
- 真实大模型接入代码到位（待写入真实 key 做最终闭环验收）

待继续推进（第三周最终验收必做）：
- 配置真实 `OPENAI_API_KEY` 并完成完整链路联调：上传CSV → 入库 → 发消息 → LLM选工具 → MCP调用 → 回复写入 messages
- 前端对接第三周后端统一响应格式（如需）
- 按GitHub方案进行镜像搬运与提交记录（docs/third_week 与 mcp/third_week）

