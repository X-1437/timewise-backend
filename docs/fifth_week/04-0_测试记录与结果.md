# 第五周测试记录与结果（阶段性）

## 1. 测试范围说明

本次测试聚焦第五周新增/改动能力的“可验证闭环”：

- 方案 1（后端生成 PNG → evidence 落盘 → MongoDB artifacts 记录元数据 → Markdown 引用图片下载 URL）
- MCP 工具输出插图（EDA / 预处理 / 特征分析 / 朴素预测）
- 图片下载 API 可用性
- 前端 Markdown 图片渲染（支持 `![]()`）

说明
- 当前环境 `mongod` 不在 PATH（无法通过命令直接查看版本），但 MongoDB 服务可正常连接（localhost:27017 ping 成功）。
- 已通过“静态检查 + 单元测试（FakeDB）+ 前端 build + 后端联调冒烟测试”验证本次改动的核心逻辑正确性。

## 2. 环境与依赖检查

### 2.1 MongoDB 可用性

检查命令

```bash
where mongod
mongod --version
```

结果
- `where mongod` 无输出
- `mongod --version` 报错：无法识别 `mongod`

连通性验证（不依赖 mongod 命令）

```bash
python -c "from pymongo import MongoClient; c=MongoClient('mongodb://localhost:27017',serverSelectionTimeoutMS=2000); c.admin.command('ping'); print('MONGO_OK')"
```

结果
- 输出：`MONGO_OK`

结论
- MongoDB 服务可用，但 `mongod` 可执行文件未加入 PATH。

### 2.2 Docker 可用性

检查命令

```bash
docker --version
```

结果
- 报错：无法识别 `docker`

结论
- 当前环境无法用 Docker 快速拉起 MongoDB 容器用于联调。

## 3. 后端测试

### 3.1 静态语法检查（compileall）

目标
- 确保所有 Python 文件语法正确、可被解释器编译加载。

执行命令

```bash
cd D:\trae_workspace\data\github\timewise-backend\mcp\fourth_week\code
python -m compileall -q .
```

结果
- 退出码：0
- 结论：通过

### 3.2 单元测试（unittest：图片 artifacts + 图片下载）

目标
- 验证图片落盘与 artifacts 落库逻辑可用（使用 FakeDB，不依赖 Mongo 服务）。
- 验证图片下载接口在 200/404 场景下行为正确（FastAPI TestClient + FakeDB）。

测试用例
- `test_save_image_png_writes_artifact`：保存 PNG 后 artifacts 写入 `type=image_png/path/displayName/sessionId/fileId`
- `test_download_image_ok`：存在 artifact 时返回 200 且 `content-type=image/png`
- `test_download_image_404`：不存在 artifact 时返回 404

执行命令

```bash
cd D:\trae_workspace\data\github\timewise-backend\mcp\fourth_week\code
python -m unittest -q
```

结果
- 用例数：3
- 通过：3
- 失败：0
- 结论：通过

代码位置
- 测试文件：[test_image_artifacts.py](file:///D:/trae_workspace/data/github/timewise-backend/mcp/fourth_week/code/tests/test_image_artifacts.py)

### 3.3 依赖一致性检查（pip check）

目标
- 检查当前 Python 环境中依赖是否存在冲突。

执行命令

```bash
python -m pip check
```

结果
- exit code：1
- 报告多项依赖冲突（主要来自环境层面的 Anaconda 包组合，而非本项目 requirements 锁定）

结论
- 该结果不作为本项目“功能正确性”判定依据，但提示当前环境并非干净的虚拟环境。
- 建议后续联调/UAT 前以项目 requirements 创建独立虚拟环境，避免隐性依赖问题。

## 4. 前端测试

### 4.1 构建测试（TypeScript + Vite build）

目标
- 确保前端类型检查通过，且构建产物可生成。

执行命令

```bash
cd D:\trae_workspace\data\github\timeswise
npm run build
```

结果
- exit code：0
- 结论：通过

## 5. 未完成的测试（阻塞项）

### 5.1 集成测试 / 联调测试（已完成：后端接口冒烟）

原计划覆盖（质量体系）
- 上传 CSV（/files/upload）
- 发送对话（/chat/messages）触发 MCP 工具
- 生成图片 artifacts，并通过 Markdown 在右侧预览渲染图片
- 导出报告（/report/download）并校验报告内容含图片引用

实际执行方式
- 启动后端服务：`python backend.py`
- 运行冒烟脚本：`python scripts/smoke_images.py`
- 覆盖工具：EDA / 预处理 / 特征分析 / 朴素预测 / 导出报告
- 覆盖图片：生成并从下载接口拉取校验 `content-type=image/png`
- 覆盖异常：随机 image_id 下载返回 404

执行命令

```bash
cd D:\trae_workspace\data\github\timewise-backend\mcp\fourth_week\code
python backend.py
python scripts/smoke_images.py
```

执行结果（脚本输出）

```text
eda ok 1
preprocess ok 2
feature ok 6
forecast ok 1
export ok 10
404 ok
ALL_OK
```

## 6. 结论

- 本次变更的“图片能力核心闭环”（生成 PNG → artifacts 记录 → 图片下载接口 → Markdown 引用）已完成单元级验证与接口联调冒烟验证，并通过前后端构建校验。
- 后续仍需补齐“页面级 UAT 手工勾检”（上传、对话、右侧预览渲染、导出按钮下载、性能计时、兜底场景）以满足质量体系的 100% 验收口径。
